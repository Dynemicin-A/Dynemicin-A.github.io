# Xuanhao Zhang — Personal Site

A static GitHub Pages portfolio focused on generative models, multimodal learning, machine learning competitions, and AI systems.

## Local preview

```bash
cd /Users/z/Documents/Codex/2026-08-31/w/outputs/xuanhao-zhang.github.io
python3 -m http.server 8080
```

Open `http://localhost:8080` in a browser.

## Deployment

The site is deployed from the `main` branch of the `Dynemicin-A/Dynemicin-A.github.io` repository using GitHub Pages. No build step is required.

Before sharing the site, customize any contact links or profile links in `index.html` if they change.
